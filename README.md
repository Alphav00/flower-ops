# 🌷 flower-ops
> *just tending garden*

Covert automation agency. Distributed nodes. Quiet growth.

## 🤖 AGENT ROUTING
| Call Sign | Role | Reads |
|-----------|------|-------|
| `---<Q>---` | Strategy, audits, architecture | `roots/`, `field-notes/`, `rhizome/` |
| `---<#>---` / `#ROVER6` | Execution, deploys, terminal | `roots/`, `tools/`, `thorns/`, `greenhouse/` |
| `---g---` | Bulk text, formatting, data | `field-notes/`, `seedbed/` |
| `---<X>--- Lady Chi` | Command, field presence, final authority | All |

## 📁 STRUCTURE
- `rhizome/` — Shared state, routing tables, env templates
- `roots/` — Constitutions, core protocols, auth rules
- `stems/` — Agent specs, handoff formats, prompt templates
- `greenhouse/` — Dev env, Docker, local models, wrangler
- `seedbed/` — Sandbox, experiments, prompt iteration
- `tools/` — CLI scripts, automation, verification preambles
- `thorns/` — Security, kill-switches, audit trails
- `field-notes/` — Docs, goals, routing tables, change logs
- `transplant/` — Prod configs, CI/CD, release manifests

## 🤖 AGENT BOOT SEQUENCE
1. Read `roots/claude.md` before any action.
2. Check `field-notes/MODEL_ROUTING.md` for task routing & cost tiers.
3. Verify all Tier 2/3 commands with Q-Triangle preamble.
4. On exit: log to `rhizome/handoff_log/` as `{agent, date, completed, blocked, next}`.
5. Never modify constitutions without `⬆️ ESCALATE TO Q`.

## 🚀 QUICK START
```bash
git clone <repo-url>
cp .env.example .env
# Read Q.md → route task → execute → log → push
```

## 🔒 SAFETY & CONVENTIONS
- No secrets in repo. Use `.env` + local vault.
- `ABORT` = immediate halt. `⬆️ ESCALATE TO Q` = pause for strategy.
- Keep files < 50KB for AI context windows. Split large configs.
- Use standard Markdown/YAML. No ambiguous formatting.

*Roots deep. Bloom unseen.*
